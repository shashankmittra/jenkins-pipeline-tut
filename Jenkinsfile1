#!/usr/bin/env groovy

@Library('shared-library-jenkins') _

pipeline{
         agent any
        
        stages{                 
                 stage ('Check logs') {
                    steps {
                        functions (1,4)
                    }
                }
		
           }	       	     	         
}
